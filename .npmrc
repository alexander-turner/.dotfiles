# Security: block postinstall scripts by default.
# Packages needing native builds (esbuild, sharp, etc.) must be
# allowlisted per-project via pnpm's onlyBuiltDependencies.
ignore-scripts=true

# Delay installing any package version until 3 days after publication.
# Most malicious packages are caught and pulled within hours.
min-release-age=3d
